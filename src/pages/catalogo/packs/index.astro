---
import Layout from '../../../layouts/Layout.astro';
import SectionTitle from '../../../components/SectionTitle.astro';
import Breadcrumb from '../../../components/Breadcrumb.astro';
import CatalogPackContainer from '../../../components/catalog/CatalogPackContainer.astro';
import PromoSection from '../../../components/catalog/PromoSection.astro';
import FAQ from '../../../components/FAQ.astro';
import CTABanner from '../../../components/CTA-Banner.astro';
import CategoryCards from '../../../components/catalog/CategoryCards.astro';
import '../../../styles/catalog.css';

// Import types, utilities and data
import type { Pack } from '../../../types/pack';
import { BookLevel, FormatTag } from '../../../types/book';
import { getProductsByType } from '../../../utils/listProducts';

// Get filter parameters from URL if any
const { searchParams } = Astro.url;
const level = searchParams.get('level') || 'all';
const format = searchParams.get('format') || 'all';
const sort = searchParams.get('sort') || 'featured';

// Define type-safe mapping objects
const levelNames: Record<string, string> = {
  [BookLevel.BASIC]: 'Nivel Básico',
  [BookLevel.INTERMEDIATE]: 'Nivel Intermedio',
  [BookLevel.ADVANCED]: 'Nivel Avanzado',
  [BookLevel.PROFESSIONAL]: 'Nivel Profesional',
  [BookLevel.ALL_LEVELS]: 'Multi-nivel',
  [BookLevel.INTERNATIONAL_EXAM]: 'Examen Internacional',
};

const formatNames: Record<string, string> = {
  [FormatTag.PDF]: 'PDF',
  [FormatTag.WORKBOOK]: 'Workbooks',
  [FormatTag.AUDIO]: 'Material con Audio',
  [FormatTag.VIDEO]: 'Material con Video',
  [FormatTag.SOFTWARE]: 'Software',
  [FormatTag.EXAMS]: 'Exámenes de Práctica',
};

// Determine page title and description based on filters
let pageTitle = 'Packs de Libros en Inglés';
let pageDescription =
  'Explora nuestros packs de libros en inglés con materiales complementarios. Ahorra con nuestros paquetes especiales diseñados para maximizar tu aprendizaje.';
let canonicalUrl = new URL('/catalogo/packs', Astro.site).toString();
let categoryHeroImage = '/images/book-sample/pack-hero.jpg';

// If level filter is applied, customize title
if (level !== 'all') {
  const levelName = levelNames[level] || level;
  pageTitle = `Packs de Inglés - ${levelName}`;
  pageDescription = `Conjuntos de materiales educativos en inglés para ${levelName}. Encuentra packs completos para tu nivel de aprendizaje.`;
  canonicalUrl = new URL(`/catalogo/packs?level=${level}`, Astro.site).toString();
}

// If format filter is applied, customize further
if (format !== 'all') {
  const formatName = formatNames[format] || format;
  pageTitle = `${formatName} - ${pageTitle}`;
  pageDescription = `${formatName} ${pageDescription}`;
  canonicalUrl = new URL(`/catalogo/packs?format=${format}`, Astro.site).toString();
}

// Get packs data using the utility function
const allPacks = getProductsByType('pack');

// Convert to Pack[] type and ensure all required fields exist
const safePacks: Pack[] = allPacks.map((pack) => ({
  id: pack.id,
  title: pack.title,
  description: pack.description || '',
  price: pack.price,
  coverImage: pack.coverImage,
  altText: pack.altText || `${pack.title} Pack cover`,
  includedItems: 'includedItems' in pack ? (pack.includedItems as string[]) : [],
  booksIds: 'booksIds' in pack ? (pack.booksIds as string[]) : [],
  rating: pack.rating || { score: 0, reviewCount: 0 },
  buyLink: 'buyLink' in pack ? (pack.buyLink as string) : `/checkout/${pack.id}`,
  detailsLink: pack.detailsLink || `/catalogo/packs/${pack.id}`,
  discount: pack.discount || 0,
  level: (pack.level as BookLevel) || BookLevel.INTERMEDIATE,
  featured: pack.featured || false,
  formatTags: pack.formatTags || [],
  popularityTags: pack.popularityTags || [],
  images: 'images' in pack ? (pack.images as string[]) : [],
  video: 'video' in pack ? (pack.video as string) : '',
}));

// Define breadcrumb items
const breadcrumbItems = [
  { label: 'Inicio', url: '/' },
  { label: 'Catálogo', url: '/catalogo' },
  { label: 'Packs', url: '/catalogo/packs', active: true },
];

// Structured data for SEO
const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: pageTitle,
  description: pageDescription,
  url: canonicalUrl,
  hasPart: safePacks.map((pack) => ({
    '@type': 'Product',
    name: pack.title,
    description: pack.description,
    url: new URL(`/catalogo/packs/${pack.id}`, Astro.site).toString(),
    image: pack.coverImage,
    offers: {
      '@type': 'Offer',
      price: pack.discount ? pack.price * (1 - pack.discount / 100) : pack.price,
      priceCurrency: 'PEN',
      availability: 'https://schema.org/InStock',
    },
  })),
};
---

<Layout
  title={`${pageTitle} | FluentReads`}
  description={pageDescription}
  canonicalURL={canonicalUrl}
  structuredData={structuredData}
>
  <!-- Hero Banner -->
  <section
    className="catalog-hero bg-primary relative overflow-hidden bg-cover bg-center py-12 text-white"
    style={`background-image: linear-gradient(to bottom, rgba(30, 58, 138, 0.8), rgba(30, 58, 138, 0.5)), url(${categoryHeroImage});`}
  >
    <div className="from-primary-dark/80 to-primary/40 absolute inset-0 bg-gradient-to-r"></div>
    <div className="relative z-10 container mx-auto px-8">
      <div className="grid h-full grid-cols-1 gap-8 md:grid-cols-3">
        <!-- Text content -->
        <div className="text-center md:col-span-2 md:text-left">
          <!-- Breadcrumbs -->
          <Breadcrumb items={breadcrumbItems} class="mb-6 text-white/90" />
          <div>
            <div
              className="fade-in mb-4 inline-block rounded-lg bg-white/10 p-4 text-4xl shadow-lg backdrop-blur-sm"
            >
              📚
            </div>
            <h1 className="slide-in-left mb-4 text-3xl font-bold md:text-4xl lg:text-5xl">
              Packs de Libros en Inglés
            </h1>
            <p className="slide-in-left mb-6 max-w-2xl text-lg text-white/90 delay-100 md:text-xl">
              Ahorra con nuestros paquetes especiales que incluyen libros, materiales digitales y
              recursos adicionales diseñados para maximizar tu aprendizaje del inglés.
            </p>
            <div
              className="slide-in-left flex flex-wrap justify-center gap-4 delay-200 md:justify-start"
            >
              <a
                href="#catalog-section"
                className="bg-secondary hover:bg-secondary-dark text-primary-dark rounded-lg px-6 py-3 font-bold shadow-lg transition-all duration-300 hover:shadow-xl"
              >
                Ver Packs
              </a>
              <a
                href="#contact-form"
                className="rounded-lg bg-white/20 px-6 py-3 font-semibold text-white backdrop-blur-sm transition-all duration-300 hover:bg-white/30"
              >
                Consultar Disponibilidad
              </a>
            </div>
          </div>
        </div>

        <!-- Decorative element -->
        <div
          className="fade-in relative hidden items-center justify-center overflow-hidden rounded-lg border-4 border-white/20 shadow-2xl delay-300 md:flex"
        >
          <div
            className="from-primary/40 to-accent/30 flex aspect-[3/4] h-full w-full items-center justify-center bg-gradient-to-br backdrop-blur-sm"
          >
            <img
              src="/images/book-sample/book-sample-01.jpg"
              alt="Muestra de pack de libros de inglés"
              className="h-full w-full object-cover"
            />
          </div>
        </div>
      </div>
    </div>

    <!-- Decorative shapes -->
    <div
      className="absolute right-0 -bottom-8 left-0 h-16 bg-white"
      style="clip-path: polygon(0 0, 100% 100%, 100% 100%, 0% 100%);"
    >
    </div>
    <div
      className="absolute right-0 -bottom-8 left-0 h-16 bg-white/20 backdrop-blur-sm"
      style="clip-path: polygon(0 0, 100% 100%, 100% 100%, 0% 100%);"
    >
    </div>
  </section>

  <!-- Main Catalog Section -->
  <section className="catalog-section relative py-12" id="catalog-section">
    <div className="catalog-pattern absolute inset-0 opacity-[0.03]"></div>
    <div className="relative z-10 container mx-auto px-4">
      <!-- Main catalog container component -->
      <CatalogPackContainer
        packs={safePacks}
        initialLevel={level}
        initialFormat={format}
        initialSort={sort}
        gridCols={3}
        productSize="medium"
      />
    </div>
  </section>

  <!-- Related Categories Section -->
  <section className="bg-neutral-light py-12">
    <div className="container mx-auto px-4">
      <div className="mb-10 text-center">
        <SectionTitle>Explora otras categorías</SectionTitle>
        <p className="mx-auto max-w-3xl text-gray-600">
          Descubre nuestra selección completa de materiales educativos en inglés
        </p>
      </div>
      <CategoryCards />
    </div>
  </section>

  <!-- Promotional Section -->
  <PromoSection
    title="Ofertas y Promociones Exclusivas"
    subtitle="Ofertas Especiales"
    className="from-primary/5 to-accent/5 bg-gradient-to-br py-16"
    countdownDays={5}
    cardSize="large"
  />

  <!-- FAQ Section -->
  <FAQ
    className="py-16"
    dataset="catalog"
    title="Preguntas frecuentes sobre nuestros packs"
    description="Resolvemos tus dudas sobre nuestros paquetes de materiales en inglés."
  />

  <!-- CTA Banner -->
  <div className="container mx-auto mb-16 px-4">
    <CTABanner type="default" />
  </div>
</Layout>

<style>
  .catalog-hero {
    background-position: center;
    background-size: cover;
    position: relative;
  }

  .catalog-pattern {
    background-image: url('/pattern.svg');
    opacity: 0.2;
  }

  /* Animation Classes */
  .slide-in-left {
    animation: slideInLeft 0.8s ease forwards;
    opacity: 0;
    transform: translateX(-20px);
  }

  .delay-100 {
    animation-delay: 0.1s;
  }

  .delay-200 {
    animation-delay: 0.2s;
  }

  .delay-300 {
    animation-delay: 0.3s;
  }

  .fade-in {
    animation: fadeIn 1s ease forwards;
    opacity: 0;
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-20px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Smooth scroll to sections when clicking navigation links
    document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
      anchor.addEventListener('click', function (this: HTMLAnchorElement, e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        if (targetId) {
          const targetElement = document.querySelector(targetId);
          if (targetElement) {
            targetElement.scrollIntoView({
              behavior: 'smooth',
              block: 'start',
            });
          }
        }
      });
    });
  });
</script>
